## Codebase Patterns

- Rust edition 2024 uses `extern crate` not needed; crate dependencies are auto-available.
- Module files go in `src/` with `pub mod` declarations in `lib.rs`.
- Sink trait uses `Send + Sync` bounds for thread-safe dispatch from backend.
- Logger and sinks use `Arc<dyn Sink>` for shared ownership.
- Thread-local producer uses `UnsafeCell` (not `RefCell`) — safe because single-thread + non-reentrant.

---

## [2026-05-04] — Phase 2.7: Hot Path Optimization (Level 1)

- Replaced `RefCell<ThreadContext>` with `UnsafeCell<ThreadContext>` in thread_local! — eliminates borrow counter overhead (~2ns saved)
- Added `SpscProducer::push_encoded()` — encode log data directly into ring buffer, eliminating stack buffer + memcpy
  - Fast path: data fits contiguously → direct encode into ring buffer memory
  - Slow path (rare): ring buffer wrap-around → encode to temp vec + copy to both halves
- Added `ThreadContext::push_encoded()` — exposes direct-encode API to macros
- Rewrote `log_impl!` macro: removed 512-byte stack buffer, removed `if _total_msg <= 512` branch, now calls `push_encoded` with closure
- Changed ring buffer entry alignment from 8-byte to 64-byte (cache line): `CACHE_LINE = 64`, `align_up()` helper
  - Prevents false sharing between adjacent log entries when backend reads them
  - Updated both `push()`, `push_encoded()`, and `pop()` to use same alignment
- Added `TODO(tsc)` comment in macros.rs for future `_rdtsc()` timestamp (estimated +5-15ns)
- Fixed pre-existing flaky tests: `abandoned_queue_cleanup` and `push_from_multiple_threads` drained stale state from global CONSUMER_REGISTRY
- Files: `src/queue.rs`, `src/thread_context.rs`, `src/macros.rs`
- **Learnings for future iterations:**
  - `UnsafeCell` is safe for thread-locals when access is non-reentrant and single-owner
  - The `thread_local!` with() closure overhead (~1ns) remains — can be eliminated with `Cell` + `const` TLS in Phase 3
  - Direct ring buffer encoding eliminates both stack zero-init and memcpy (~3-4ns total)
  - Cache line alignment (64B) wastes up to 56 bytes per entry but prevents false sharing — worth it for NUMA
  - `rtrb::WriteChunkUninit::as_mut_slices()` returns split slices at wrap-around boundary; fast path checks `total <= first.len()` for contiguity
  - The `push()` and `pop()` MUST agree on `align_up()` calculation, otherwise reads misalign

**Remaining optimization notes (updated):**
- `THREAD_CTX.with(|ctx| ...)` closure overhead ~1ns — Phase 3: `Cell`-based const TLS for local-exec model
- Timestamp still hardcoded to 0 — Phase 3: TSC via `std::arch::x86_64::_rdtsc()`
- Custom SPSC ring buffer with explicit memory ordering could replace `rtrb` for more control
- Ring buffer `write_chunk_uninit()` still zero-inits via `write_bytes()` — could skip if we ensure all bytes written

**Benchmark results (2.7 vs 2.0):**
- 1 integer: 10.4 ns → 3.35 ns (3.1×, -68%)
- 2 floats: 11.5 ns → 3.63 ns (3.2×, -68%)
- 3 strings: 15.3 ns → 5.22 ns (2.9×, -65%)
- 1 Vec<String>: 204 ns → 31.9 ns (6.4×, -83%)
---
## [2026-05-04] — 1.1 Project Scaffold
- Added dependencies to Cargo.toml: `rtrb`, `rkyv` (with `std` feature), `rkyv_derive`, `parking_lot`, `anstream`, `is-terminal`, `time`, `thiserror`
- Created `src/lib.rs` with `pub mod` declarations for all planned modules
- Created stub files: `level.rs`, `metadata.rs`, `queue.rs`, `sink.rs`, `sinks/mod.rs`, `sinks/console.rs`, `logger.rs`, `message.rs`, `thread_context.rs`, `backend.rs`, `frontend.rs`, `macros.rs`
- Removed `src/main.rs` (this is a lib crate)
- All quality checks pass: cargo check, cargo test, cargo clippy, cargo fmt
- **Learnings for future iterations:**
  - `rkyv` 0.7 does NOT have a `derive` feature — derive macros come from separate `rkyv_derive` crate
  - `VecDeque` does not have `extend_from_slice` — use `extend(iter)` instead
  - Clippy `new_without_default` requires `Default` impls for types with `new()`
  - Rust edition 2024 reorders imports differently than 2021 (alphabetical by default)
---

## [2026-05-04] — Phase 2 (2.0–2.6): Zero-Allocation Hot Path
- Implemented LogArg trait with packed encoding (tags first, then all payloads) in src/arg.rs
- Supports i32, i64, u32, u64, usize, f32, f64, bool, &str, String with 1-byte tag + fixed/variable payload
- Added DisplayArg<T> and DebugArg<T> wrappers for fallback types (pre-format via Display/Debug)
- DecodedArg enum with as_display_string() + single generic decode_args() for backend
- format_with_args() replaces {} placeholders with decoded args
- Changed queue header from 4-byte to 8-byte (u32 length + u32 flags) for 8-byte alignment
- push() uses write_chunk_uninit + as_mut_slices + raw pointer zero-init for safety
- pop() now reads 8-byte headers and pads to 8-byte boundary
- Changed LogMessage from args_data: Vec<u8> to args_len: u16 (inline args follow header)
- serialize_header_into() writes raw bytes matching rkyv archive layout (32 bytes)
- decode() uses raw pointer cast instead of check_archived_root (avoids CheckBytes issues in edition 2024)
- Rewrote all 8 macros with new pattern: ($logger, $fmt:literal $(, $arg:expr)*)
- Hot path: compute sizes via log_max_size(), use 512-byte stack buffer or vec! fallback
- Encodes count byte + packed tags + sequential payloads into buffer
- Backend format_message() now decodes args from inline bytes via decode_args() then format_with_args()
- Updated integration tests for new macro signature (explicit arg syntax)
- Files: src/arg.rs (new), src/queue.rs, src/message.rs, src/macros.rs, src/backend.rs, src/lib.rs, tests/integration.rs, benches/logging.rs
- **Benchmark results:**
  - 1 integer: 121 ns → 10.4 ns (11.6×)
  - 2 floats: 249 ns → 11.5 ns (21.7×)
  - 3 strings: 237 ns → 15.3 ns (15.5×)
  - 1 Vec<String> (DebugArg): 316 ns → 204 ns (1.55×)
- **Learnings for future iterations:**
  - rkyv 0.7 lacks FallibleSerializer — had to use manual byte writing for header serialization
  - check_archived_root with CheckBytes derive has issues in edition 2024 — switched to raw pointer access
  - Declarative macros can't generate unique variable names — each arg's encoding must happen in separate {} blocks
  - rtrb's WriteChunkUninit lacks as_mut_ptr; used as_mut_slices + raw pointer zero-init pattern
  - The macro evaluates $arg multiple times — users must pre-bind rvalue args (e.g., DebugArg(foo())) to a local
  - Stack buffer approach (512 bytes) achieves zero-allocation for common case, vec! fallback for large messages
  - Packed tags-before-payloads layout proven efficient and simple to decode
  - 10 ns target almost met — remaining overhead is ring buffer copy (~2ns), level check (~1ns), encoding (~5ns)
---

## [2026-05-04] — Code Review Fixes (issues #1, #2, #5)
- Fixed HIGH-severity UB: unaligned pointer dereference in `LogMessage::decode()` replaced with `std::ptr::read_unaligned` on individual fields
- Added `#[repr(C)]` to `LogMessage` to guarantee field layout in `ArchivedLogMessage` (rkyv carries repr through)
- Added compile-time assertion: `const _: () = assert!(ARCHIVED_HEADER_SIZE == 32)`
- `ARCHIVED_HEADER_SIZE` now computed from `size_of::<ArchivedLogMessage>()` instead of hardcoded magic number
- `decode()` now returns owned `ArchivedLogMessage` instead of `&ArchivedLogMessage` — eliminates unsound lifetime coupling to raw buffer
- Refactored `format_message()` to accept `args_bytes: &[u8]` parameter instead of unsafe pointer math past struct boundary
- Backend caller now extracts args slice explicitly: `raw[ARCHIVED_HEADER_SIZE..][..header.args_len as usize]`
- Refactored 8 log macros (550 lines) to delegate to single `log_impl!` helper macro (172 lines) — 420-line reduction
- Files changed: `src/message.rs`, `src/backend.rs`, `src/macros.rs`
- **Learnings for future iterations:**
  - `Vec::with_capacity(n)` does NOT guarantee alignment beyond 1 byte — raw casts to larger aligned types are UB
  - `#[repr(C)]` on the source struct carries through to rkyv's `ArchivedLogMessage` generate type
  - Un-exported helper macros can't be called via `$crate::name` from exported macros — must use `#[macro_export] #[doc(hidden)]`
---
